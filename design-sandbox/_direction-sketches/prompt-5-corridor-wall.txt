A browse-page UI sketch in the exact same visual world as the attached reference: aged cream survey-paper background, sepia ink linework, a red wax verification-seal accent, terracotta and teal zone-coded accents, slab-serif display type, plain sans body type, typewriter-mono labels and numbers, thin hairline rules, minimal border radius.

Layout to depict: at the very top, a search input pill labeled "Search spaces". Below it, several stacked horizontal rows, each row is a named corridor/locality (e.g. one row labeled "WHITEFIELD", one labeled "KORAMANGALA", one labeled "OUTER RING ROAD" in small tracked monospace caps as a row header), and each row scrolls horizontally with 4-5 small rectangular listing tiles (thumbnail photo, small plot-number tag, short title). One tile in the middle row is shown larger/focused, slightly lifted with a highlighted border, while the tiles around it are slightly dimmed.

Style: flat matte design sketch, deliberately unfinished, no photorealism, no gloss, landscape 16:9 desktop web viewport, no browser chrome. All text except the corridor names, plot-number tags, and "Search spaces" placeholder should be greeked as illegible wavy placeholder lines, not real words.
