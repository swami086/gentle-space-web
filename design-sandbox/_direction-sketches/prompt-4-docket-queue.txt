A browse-page UI sketch in the exact same visual world as the attached reference: aged cream survey-paper background, sepia ink linework, a red wax verification-seal accent, terracotta and teal zone-coded accents, slab-serif display type, plain sans body type, typewriter-mono labels and numbers, thin hairline rules, minimal border radius.

Layout to depict: at the very top, a search input pill labeled "Search spaces" with a small compass-icon button. Below it, a compact horizontal strip showing a small cadastral-style plot map with 6-8 small numbered plot icons in a row (a wayfinding index, not a large map panel). Below that, a single full-width vertical column of listing rows styled as filed registry docket entries: each row has a small thumbnail photo on the left, a "DOCKET NO. 10xx" monospace tag, a bold title, a locality line, and a short teaser line. The second row in the list is shown expanded (accordion open), revealing inside it: a short description paragraph, a row of amenity pill tags, a small inset approximate-area map circle, and a "Why this fits" collapsible label. Every other row stays collapsed/closed.

Style: flat matte design sketch, deliberately unfinished, no photorealism, no gloss, landscape 16:9 desktop web viewport, no browser chrome. All text except the docket numbers and section labels should be greeked as illegible wavy placeholder lines, not real words.
